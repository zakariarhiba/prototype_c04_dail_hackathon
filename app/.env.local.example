DATABASE_URL=postgres://c04:c04@localhost:5433/c04

# Signs the session cookie (app/app/lib/auth.ts). A dev default is baked in
# so the app runs without this being set; override for anything longer-lived
# than this prototype.
# SESSION_SECRET=change-me
