# Evidence View and Failure Case

Not started. Fill in once the build produces a real evidence view.

## Evidence view

Screenshot or transcript of the reconciliation evidence trail (per-DN lines
explaining a discrepancy), plus a short note on which parts of it are
computed live vs. simulated. See `01-system-design.md` §6 for the
real-vs-simulated split this should match.

## Uncertain / failure case

At least one case where the system is deliberately uncertain and refuses to
guess (per `01-system-design.md` §4, the AMBIGUOUS classification path).
Show: what triggered it, what the system said, what the human had to decide,
and what state resulted.
